[Unit]
Before=flirapp.service
After=weston.service

[Service]
Type=simple
ExecStart=/bin/bash -c '/sbin/modprobe okeanos && /bin/sleep infinity'
ExecStop=/bin/bash -c '/sbin/modprobe -r okeanos'

[Install]
WantedBy=graphical.target
