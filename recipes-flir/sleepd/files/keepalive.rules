# There are a number of modifiers that are allowed to be used in some
# of the different fields. They provide the following subsitutions:
#
# %n the "kernel number" of the device.
#    For example, 'sda3' has a "kernel number" of '3'
# %e the smallest number for that name which does not matches an existing node
# %k the kernel name for the device
# %M the kernel major number for the device
# %m the kernel minor number for the device
# %b the bus id for the device
# %c the string returned by the PROGRAM
# %s{filename} the content of a sysfs attribute
# %% the '%' char itself
#

#fxos8700 compass/accelerometer chip is not supposed to be a keepalive source
#I wish i could use != for comparison...
#SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="fxos8700 Mag", SYMLINK+="input/wakeup/%k"
#SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="fxos8700 Acc", SYMLINK+="input/wakeup/%k"
#SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="Laser distance meter", SYMLINK+="input/wakeup/%k"
SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="da9063-onkey", SYMLINK+="input/wakeup/%k"
SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="EP0510M09", SYMLINK+="input/wakeup/%k"
SUBSYSTEM=="input", KERNEL=="event[0-9]*", ATTRS{name}=="erla-keyboard.14", SYMLINK+="input/wakeup/%k"
